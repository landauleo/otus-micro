micronaut:
  server:
    port: 8000
  application:
    name: microHomework
netty:
  default:
    allocator:
      max-order: 3
datasources:
  default:
    url: jdbc:postgresql://localhost:5432/postgres
    driverClassName: org.postgresql.Driver
    username: ruanaaf
    password: ANYA_miupiu111
    schema-generate: CREATE_DROP
    dialect: POSTGRES

jpa:
  default:
    entity-scan:
      packages: 'leo.landau'

#logger:
#  levels:
#    io.micronaut.context.condition: TRACE